{
  "name": "ai-fitness-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "blockchain"
  ],
  "scripts": {
    "start:client": "npm run dev --workspace=client",
    "start:server": "npm run dev --workspace=server",
    "start:blockchain": "npm run node --workspace=blockchain",
    "install:all": "npm install"
  },
  "dependencies": {
    "openai": "^6.9.1"
  }
}
